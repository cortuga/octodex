{
  "private": true,
  "name": "octodex",
  "scripts": {
    "start": "browser-sync start --server public --files public",
    "deploy": "netlify deploy --prod --dir=public",
    "setup-deploy": "app-app --deploy && yarn deploy",
    "postdeploy": "echo 'Your site was deployed to https://octodex-cgsus.netlify.com'"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "stylelint": "^10.1.0",
    "stylelint-config-standard": "^18.3.0"
  },
  "dependencies": {
    "update": "^0.7.4"
  },
  "homepage": "https://octodex-cgsus.netlify.com"
}